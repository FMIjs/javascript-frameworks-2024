import { TextOverflowDirective } from './text-overflow.directive';

describe('TextOverflowDirective', () => {
  it('should create an instance', () => {
    const directive = new TextOverflowDirective();
    expect(directive).toBeTruthy();
  });
});
